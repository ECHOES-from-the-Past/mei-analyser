{
  "name": "mei-analyser",
  "private": true,
  "version": "0.1.5-dev",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": " vite preview",
    "docs": "jsdoc --destination ./docs --readme README.md src/*.js",
    "docs-production": "jsdoc --destination ./dist/docs --readme README.md src/*.js",
    "build-production": "vite build --base=/mei-analyser/ && npm run docs-prod"
  },
  "devDependencies": {
    "jsdoc": "^4.0.2",
    "vite": "^5.0.8"
  }
}
