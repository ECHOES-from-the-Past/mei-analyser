{
  "name": "mei-analyser",
  "version": "0.3.0",
  "devDependencies": {
    "vite": ">=5.2.6"
  },
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/mei-analyser/",
    "database": "node src/database/updateDatabase.mjs",
    "preview": "NODE_ENV=production npm run database && vite preview --base=/mei-analyser/",
    "algorithm": "node src/analysis/test-algorithm.js"
  },
  "type": "module",
  "dependencies": {
    "@octokit/core": "^6.1.2",
    "xml2js": "^0.6.2"
  }
}
